# Example environment variables
# Set a strong ADMIN_API_KEY (used to protect upload endpoint)
ADMIN_API_KEY=your-secret-api-key

# Optional but recommended: 32+ character secret for encrypting gallery.json
# Use a securely generated secret. If empty, gallery.json will be stored unencrypted.
DATA_KEY=replace-with-very-strong-secret
